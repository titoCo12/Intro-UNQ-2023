function hayBolitas_Al_(color, direccion) {
    /*
        PROPÓSITO: Indicar si hay una celda lindante hacia el **direccion**
        y si contiene bolitas de color **color**
        PARÁMETROS:
            *color: Color - color de las bolitas a revisar
            *direccion: Direccion - direccion de la celda la cual
            revisar si existe y contiene bolitas del color indicado
        TIPO: Booleano
        PRECONDICIONES: Ninguna
    */
    return(puedeMover(direccion) && tieneBolitas_Al_(color, direccion))
}